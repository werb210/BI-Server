import { logger } from "../platform/logger";
export async function runApolloSyncOnce(): Promise<void> { logger.info("apolloSyncJob stub — Block 2 not yet applied"); }
export function startApolloSyncJob(): void {}
